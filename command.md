```md
git init //initialize local git repo
git add <file> //Add files to index
git status //check status of working tree
git commit //commit changes in index
git push //push to remote repo
git pull //pull latest from remote repo
git clone //download remote repo to local machine
git config --global user.name "github user name" //configure github to access specific account
git config --global user.email "github account email" //configure github to access specific account
```
